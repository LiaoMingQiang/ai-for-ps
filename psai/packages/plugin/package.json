{
  "name": "@psai/plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@psai/shared": "*"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
